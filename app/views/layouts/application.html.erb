<!DOCTYPE html>
<html>
<head>
  <title>ZombieTwitterApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	
<% if @current_user %>
	<%= "You are logged in as: #{@current_user.login}" %>
	<%= link_to "Compose New Tweet", new_tweet_path %>
	<%= link_to "Logout", '/sessions/:id', method: :delete %>
<% else %>
	<%= link_to "Login", new_session_path %>
	or <%= link_to "Sign Up", new_user_path %>
<% end %>	

<%= link_to "Home", tweets_path %>

<% if flash[:notice].present? %>
	<p style="color: blue; background: yellow; padding: 6px"><%= flash[:notice] %></p>
<% end %>

<%= yield %>

</body>
</html>
